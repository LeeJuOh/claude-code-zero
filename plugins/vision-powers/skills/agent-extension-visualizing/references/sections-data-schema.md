# Agent Extension Visual — JSON Data Schema

This document defines the JSON structure for `sections-data.json`. The visual-report-writer outputs this file; the `render-sections.js` script converts it into HTML section files with correct CSS class names.

**Key principle**: Focus on content, not presentation. The render script handles all HTML structure, CSS classes, Mermaid wrappers, zoom controls, and chart containers. You only provide the data.

---

## Top-level Structure

```json
{
  "metadata": { ... },
  "source_context": { ... },
  "sections": {
    "header": { ... },
    "overview": { ... },
    "architecture": { ... },
    "feature_deep_dive": { ... },
    "environment_fit": { ... },
    "usage_guide": { ... },
    "components": { ... },
    "security_audit": { ... },
    "dependencies": { ... },
    "plugin_profile": { ... },
    "footer": { ... }
  }
}
```

---

## metadata (required)

Same fields as the existing HTML mode's metadata.json. The render script passes these through to the assembler.

| Field | Type | Description |
|-------|------|-------------|
| `lang` | string | Language code: "en", "ko", "ja", etc. |
| `title` | string | Report title, e.g. "Agent Extension Visual: plugin-name" |
| `font_link` | string | Google Fonts `<link>` tag (stylesheet only, no preconnect) |
| `css_variables` | string | CSS variable overrides: `--font-heading: ...; --font-body: ...; --accent: ...;` |
| `css_variables_dark` | string | Dark mode overrides: `--accent: ...;` |
| `mermaid_theme` | string | Additional Mermaid themeVariables (usually empty) |

**Note**: `toc_content` and `chart_data` are auto-generated by the render script from the sections data. Do NOT include them.

---

## source_context (optional)

Passed through from the orchestrator. Used for generating source links on component cards.

| Field | Type | Description |
|-------|------|-------------|
| `type` | string | `"local"` or `"github"` |
| `base` | string | Absolute path to plugin root |
| `github_url` | string | GitHub web URL base (only when type is "github") |

---

## sections.header

| Field | Type | Description |
|-------|------|-------------|
| `plugin_name` | string | Plugin display name |
| `subtitle` | string | Optional subtitle (default: "Agent Extension Visual Report") |
| `version` | string | Plugin version |
| `author` | string | Plugin author |
| `date` | string | Generation date |
| `risk_level` | string | `"critical"`, `"high"`, `"medium"`, or `"low"` |
| `license` | string | License identifier |
| `keywords` | string | Comma-separated keywords |

---

## sections.overview

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading (default: "Plugin Overview") |
| `summary` | string | 2-3 sentence at-a-glance summary |
| `what` | string | What the plugin does |
| `how` | string | How it works |
| `unique` | string | What makes it different |
| `features` | string[] | Key feature list. ⚠️ Plain strings only — NOT objects like `{title, description}`. Write each feature as a single sentence. |
| `kpis` | array | KPI stat cards (see below) |
| `chart` | object | Component chart data (see below) |
| `pattern` | string | Primary pattern description |
| `target_users` | string | Target user description |

### kpis[]

| Field | Type | Description |
|-------|------|-------------|
| `value` | string\|number | Display value |
| `label` | string | Label text |
| `variant` | string | `"success"`, `"danger"`, `"info"`, or `"warning"` |

### chart

| Field | Type | Description |
|-------|------|-------------|
| `type` | string | Chart.js type: `"doughnut"`, `"bar"`, `"pie"` |
| `labels` | string[] | Chart labels |
| `data` | number[] | Chart data values |
| `colors` | string[] | Optional custom colors (auto-generated if omitted) |

**⚠️ Chart labels MUST use extension types** — `["Skills", "Agents", "Commands", "Hooks", "MCP", "LSP"]` with counts from the component inventory. Do NOT use purpose/category labels (e.g., "Scaffolding", "Automation"). The chart shows the composition of the plugin by Claude Code extension type. Omit types with zero count.

---

## sections.architecture

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `narrative` | object | Problem/insight/thesis/constraints (see below) |
| `philosophy` | array | Design philosophy cards |
| `diagrams` | array | Mermaid diagrams |

### narrative

| Field | Type | Description |
|-------|------|-------------|
| `problem` | string | User pain point |
| `core_insight` | string | Non-obvious understanding |
| `design_thesis` | string | Fundamental approach |
| `constraints` | string | Intentional refusals |

### philosophy[]

| Field | Type | Description |
|-------|------|-------------|
| `name` | string | Principle name |
| `description` | string | 2-3 sentence explanation |
| `example` | string | Concrete codebase reference |

### diagrams[]

| Field | Type | Description |
|-------|------|-------------|
| `title` | string | Diagram heading (e.g. "Component Relationships") |
| `mermaid` | string | Raw Mermaid diagram code |
| `size` | string | Optional: `"compact"`, `"default"`, or `"tall"` |

**Mermaid rules still apply**: Use 8-digit hex for fills in classDef (`fill:#0891b226`), never `rgba()`. Never set `color:` in classDef. Add `click NodeId "#section-id"` for in-report navigation. Do NOT wrap mermaid code in `<pre class="mermaid">` tags — the render script adds those automatically.

**⚠️ Architecture diagram guidelines**:
- Show the **structural relationships** between layers, not a flat list of all components.
- For plugins with many components (>15), group by architectural layer (e.g., "Command Layer", "Agent Layer", "Hook Layer") and show **representative examples** (3-5 per layer) with a note like "(18 agents)".
- NEVER list all components individually or use "...N more" truncation — this produces broken layouts.
- Focus on data flow, dispatch patterns, and inter-layer connections.
- Keep total node count under 25 per diagram for readable rendering.

---

## sections.feature_deep_dive

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `lead` | string | Lead paragraph text |
| `mechanisms` | array | Mechanism cards |
| `workflow_trace` | array | Primary workflow steps |
| `tutorial_scenarios` | array | Practical guide scenarios |

### mechanisms[]

| Field | Type | Description |
|-------|------|-------------|
| `title` | string | "{Principle}: {How It's Enforced}" |
| `why_matters` | string | Problem without this mechanism. ⚠️ Must be non-empty — empty strings render as blank paragraphs. |
| `steps` | array | Implementation chain steps |
| `code_pattern` | object | Optional code snippet |
| `in_practice` | string | Real usage scenario. ⚠️ Must be non-empty. |
| `best_practice` | string | Tip for users. ⚠️ Must be non-empty. |

### mechanisms[].steps[]

| Field | Type | Description |
|-------|------|-------------|
| `text` | string | Step description. ⚠️ Must be non-empty — empty strings render as blank list items. |
| `source_link` | object | Optional: `{ "path": "relative/path", "label": "display text" }` |

### mechanisms[].code_pattern

| Field | Type | Description |
|-------|------|-------------|
| `language` | string | Programming language for syntax highlighting |
| `code` | string | Code snippet (will be HTML-escaped by render script) |

### workflow_trace[]

| Field | Type | Description |
|-------|------|-------------|
| `title` | string | Step title |
| `description` | string | Step description |
| `source_link` | object | Optional: `{ "path": "relative/path" }` |

### tutorial_scenarios[]

| Field | Type | Description |
|-------|------|-------------|
| `title` | string | Scenario title |
| `steps` | array | `[{ "user_action": "...", "behind_scenes": "..." }]`. ⚠️ Both `user_action` and `behind_scenes` must be non-empty — empty strings render as blank elements. |
| `tips` | string[] | Best practice tips |

---

## sections.environment_fit

Standalone diagnosis section. When verdict is RECOMMENDED with no findings, provide minimal data (verdict + verdict_summary only).

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `verdict` | string | `"RECOMMENDED"`, `"CONDITIONAL"`, `"REDUNDANT"`, or `"CONFLICTING"` |
| `verdict_summary` | string | 1-2 sentence diagnosis |
| `installation_status` | object | ⚠️ Must be an object: `{ "status": "NEW\|ALREADY_INSTALLED", "detail": "..." }`. NOT a plain string. |
| `context_budget` | object | See below |
| `dependency_check` | object | See below |
| `overlap` | array | Functional overlap findings |
| `trigger_collisions` | array | Trigger collision findings |
| `hook_impact` | object | Hook impact analysis |
| `component_deps` | array | Component dependency findings |
| `scope_impact` | object | Scope hierarchy impact analysis (see below) |
| `bundle_source` | object | Optional: `{ "type": "marketplace"|"local"|"github", "identifier": "..." }` — installation origin |
| `recommendations` | string[] | ⚠️ Plain strings only — NOT objects like `{priority, text}`. Each item is one recommendation sentence. |

### scope_impact

| Field | Type | Description |
|-------|------|-------------|
| `installation_scope` | string | Plugin's installation scope: `"global"` (default for marketplace plugins) |
| `affected_scopes` | string[] | Scopes affected by this plugin: `["global"]`, `["global", "workspace"]`, etc. |
| `scope_conflicts` | array | `[{ "type", "this_component", "existing_component", "scope", "detail" }]` |
| `appropriateness` | string | 1-2 sentence assessment of whether the plugin's scope is appropriate |

### context_budget

| Field | Type | Description |
|-------|------|-------------|
| `always_loaded` | object | Always-loaded token breakdown (see below) |
| `deferred` | object | Deferred token breakdown (see below) |
| `rows` | array | `[{ "resource", "current", "adding", "budget_200k", "budget_1m", "severity" }]` |
| `note` | string | Optional context budget notes |

#### always_loaded

| Field | Type | Description |
|-------|------|-------------|
| `skill_descriptions` | object | `{ "tokens": number, "items": number }` — skill/command description token cost |
| `rules` | object | `{ "tokens": number, "items": number, "always": number, "on_demand": number }` — rules with/without `paths:` |
| `claude_md` | object | `{ "tokens": number, "import_chain": number }` — CLAUDE.md + @import chain token cost |
| `total_tokens` | number | Sum of all always-loaded token costs |

#### deferred

| Field | Type | Description |
|-------|------|-------------|
| `mcp_tools` | object | `{ "tokens": number, "servers": number }` — estimated MCP tool schema tokens |
| `zero_cost_skills` | number | Count of skills with `disable-model-invocation: true` |
| `on_demand_rules` | number | Count of rules with `paths:` (deferred loading) |
| `total_tokens` | number | Sum of all deferred token costs |

### dependency_check

| Field | Type | Description |
|-------|------|-------------|
| `status` | string | ⚠️ Required. `"READY"`, `"PARTIAL"`, or `"ACTION_NEEDED"`. Infer from items if needed. |
| `severity` | string | ⚠️ Required. `"low"`, `"medium"`, or `"high"`. Must match status. |
| `items` | array | `[{ "name", "type", "required": bool, "status": "AVAILABLE\|MISSING", "help" }]` |

### overlap[]

`[{ "this_skill", "existing_skill", "classification": "DUPLICATE\|OVERLAP\|COMPLEMENT\|UPGRADE", "detail" }]`

### trigger_collisions[]

`[{ "severity": "high\|medium", "this_skill", "existing_skill", "description" }]`

### hook_impact

`{ "rows": [{ "metric", "current", "adding", "projected", "severity" }] }`

### component_deps[]

`[{ "component", "depends_on", "type", "status": "AVAILABLE\|MISSING" }]`

---

## sections.usage_guide

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `installation_cmd` | string | Installation command |
| `prerequisites` | array | `[{ "requirement": "...", "details": "..." }]` |
| `key_components` | array | `[{ "name": "...", "summary": "..." }]` |
| `when_to_use` | string[] | Use cases |
| `when_not_to_use` | string[] | Anti-use cases |

---

## sections.components

Tab-based component listing. The render script generates the tab UI automatically.

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `skills` | object | `{ "active": ComponentCard[], "reference": ComponentCard[] }` |
| `agents` | ComponentCard[] | Agent component cards |
| `commands` | ComponentCard[] | Command component cards |
| `rules` | ComponentCard[] | Rule component cards |
| `hooks` | ComponentCard[] | Hook component cards |
| `mcp` | ComponentCard[] | MCP server cards |
| `lsp` | ComponentCard[] | LSP server cards |
| `config` | object | Optional: `{ "agent": string, "raw": string }` — plugin settings.json content |

### ComponentCard

| Field | Type | Description |
|-------|------|-------------|
| `name` | string | Component name |
| `description` | string | Brief description |
| `source_path` | string | Relative path for source link |
| `meta` | array | `[{ "label": "...", "value": "..." }]` — key-value metadata rows |
| `raw_content` | string | Optional raw content excerpts (shown in collapsible) |

---

## sections.security_audit

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `risk_level` | string | `"critical"`, `"high"`, `"medium"`, or `"low"` |
| `risk_summary` | string | 1-sentence risk summary |
| `permission_matrix` | array | `[{ "component", "tools", "scope", "risk" }]` |
| `findings` | array | `[{ "severity", "title", "description", "source" }]` |

---

## sections.dependencies

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `tools` | array | `[{ "tool", "used_by", "purpose" }]` |
| `external` | array | `[{ "name", "type", "required": bool }]` |
| `env_vars` | array | `[{ "variable", "purpose", "required": bool }]` |
| `models` | array | `[{ "component", "model", "purpose" }]` |

---

## sections.plugin_profile

| Field | Type | Description |
|-------|------|-------------|
| `heading` | string | Optional section heading |
| `inventory` | array | `[{ "type", "count": number, "names" }]` |
| `category_distribution` | array | `[{ "category", "count": number, "skills", "badge_variant" }]` |
| `docs_checklist` | array | `[{ "item", "status": "pass\|fail", "notes" }]` |
| `security_summary` | string | 2-3 sentence security risk summary |
| `pattern` | string | Primary pattern |
| `target_users` | string | Target users |
| `quality_checklist` | array | `[{ "criterion", "status": "pass\|fail", "details" }]` |
| `skill_design_quality` | array | See below |
| `improvement_recommendations` | string[] | ⚠️ Plain strings only — NOT objects. Each item is one recommendation sentence. |

### skill_design_quality[]

| Field | Type | Description |
|-------|------|-------------|
| `name` | string | Skill name |
| `description` | string | `"pass"` or `"fail"` (description quality) |
| `disclosure` | string | `"pass"` or `"fail"` (progressive disclosure) |
| `gotchas` | string | `"pass"` or `"fail"` (has gotchas section) |
| `scripts` | string | `"pass"` or `"fail"` (has scripts) |
| `hooks` | string | `"pass"` or `"fail"` (has hooks) |
| `maturity` | string | Maturity level label |

---

## sections.footer

| Field | Type | Description |
|-------|------|-------------|
| `date` | string | Generation date |
| `version` | string | Report/plugin version |
