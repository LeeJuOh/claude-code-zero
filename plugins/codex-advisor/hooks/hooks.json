{
  "description": "Sets CODEX_COMPANION_TRANSCRIPT_PATH when the Official Codex plugin's own SessionStart hook is disabled, so /codex-transfer works without --source even then. Replaces a codex-advisor Verifier's prompt with its hashed, script-written payload so the main session cannot add its own words.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.mjs\"",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/verifier-payload.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
